{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "VITE_BASE44_APP_ID",
      "description": "Base44 app id, only needed to talk to a hosted Base44 backend. The app renders without it.",
      "requiredAtBoot": false
    }
  ]
}
